// next.config.js
module.exports = {
  i18n: {
    locales: ["en", "ar"], // English and Arabic
    defaultLocale: "en", // Default language
    localeDetection: true, // Optional: detect user's language preference
  },
};
